import 'package:flutter/material.dart';

class ProductListDetailScreen extends StatelessWidget {
  const ProductListDetailScreen({super.key});
  @override
  Widget build(BuildContext context) {
  return Scaffold(
    appBar: AppBar(
      title: const Text('ProductListDetailScreen'),
    ),
    body: const Center(
      child: Text('ProductListDetailScreen'),
    ),
    );
  }
}